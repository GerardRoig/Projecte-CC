# Habilitar monitoreo
http_port: 8222

# Configurar JetStream
jetstream {
  store_dir: "/data/jetstream"
}

# Configurar límites
max_payload: 10MB

authorization {
  default_permissions {
    publish = ">"
    subscribe = ">"
  }
}
